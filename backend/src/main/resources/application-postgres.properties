spring.application.name=backend
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:bpdb}
spring.datasource.username=${DB_USER:bpuser}
spring.datasource.password=${DB_PW:password}

spring.h2.console.enabled=false